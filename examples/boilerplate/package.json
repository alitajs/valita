{
  "name": "@example/boilerplate",
  "private": true,
  "scripts": {
    "analyze": "ANALYZE=1 valita build",
    "build": "valita build",
    "dev": "valita dev",
    "preview": "valita preview",
    "setup": "valita setup",
    "start": "npm run dev"
  },
  "dependencies": {
    "@vueuse/core": "^9.13.0",
    "valita": "workspace:*"
  }
}
